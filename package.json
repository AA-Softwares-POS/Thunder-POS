{
  "name": "thunderpos-cloud-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "echo 'Run workspace specific dev commands'",
    "build": "npm run build --workspace=@thunderpos/admin-panel"
  },
  "devDependencies": {
    "electron-vite": "^5.0.0"
  }
}